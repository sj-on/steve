{
  "name": "steve",
  "version": "1.0.0",
  "scripts": {
    "start": "eleventy --serve",
    "build": "rimraf public && eleventy",
    "build:serve": "npm run build && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "rimraf": "^6.1.3"
  }
}
